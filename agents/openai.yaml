interface:
  display_name: "Inspect Dependency Source"
  short_description: "Inspect the exact source used by a dependency"
  default_prompt: "Use $inspect-dependency-source to determine the exact dependency version or ref used by this project, reuse or acquire its managed source tree, inspect source_path read-only, and state the resolved version or ref and commit in the answer."
